import { header } from './Header.module.scss'
const Header = () => (
  <header className={header}>
    <h1>Digest</h1>
    <p>Aggregated collection of important news you might have missed due to COVID-19.</p>
  </header>
)

export default Header
